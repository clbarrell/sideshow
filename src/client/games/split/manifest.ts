import type { GameManifest } from "../registry";

export const manifest: GameManifest = {
  id: "split",
  name: "Split",
  tagline: "Stay with the bigger crowd before the camera cuts you loose.",
  minPlayers: 3,
  maxPlayers: 10,
  controls: "Portrait. One thumbstick: move, or aim and push the frame after you are cut.",
};
