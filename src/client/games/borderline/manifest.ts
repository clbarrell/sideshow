import type { GameManifest } from "../registry";

export const manifest: GameManifest = {
  id: "borderline",
  name: "Borderline",
  tagline: "Promise peace, secretly invade or guard, then watch every border change at once.",
  minPlayers: 10,
  maxPlayers: 10,
  controls: "Portrait: choose INVADE or GUARD, a numbered province, FORCE 1/2/3, then commit.",
};
