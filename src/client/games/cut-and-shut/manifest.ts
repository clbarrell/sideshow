import type { GameManifest } from "../registry";

export const manifest: GameManifest = {
  id: "cut-and-shut",
  name: "Cut & Shut",
  tagline: "Turn one road each. Keep all six couriers on the shared circuit.",
  minPlayers: 2,
  maxPlayers: 10,
  controls: "Portrait. One large Turn road button. Read the shared route on the projector; every survivor scores for everyone.",
};
