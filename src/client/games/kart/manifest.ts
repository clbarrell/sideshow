import type { GameManifest } from "../registry";

export const manifest: GameManifest = {
  id: "kart",
  name: "Backyard Circuit",
  tagline: "Three laps of turbo gates, bumper hits, and last-place comebacks.",
  minPlayers: 1,
  maxPlayers: 10,
  controls: "Turn sideways. Left thumb steers; right thumb holds GO. Tap BOOST when ready.",
};
